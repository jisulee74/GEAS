dataset:
  root: datasets
  raw_dir: 00_raw
  resampled_dir: 01_resampled
  split_dir: 02_split
  quality_controlled_dir: 03_quality_controlled

split:
  train_ratio: 0.70
  validation_ratio: 0.15
  test_ratio: 0.15

quality_control:
  input_dir_name: 02_split
  output_dir_name: 03_quality_controlled
  transform_splits: [train, validation]
